# Pre-Implementation Requirements Checklist: Ticket Generation Pipeline

**Purpose**: Formal pre-implementation gate — interrogate the *requirements themselves* for completeness,
clarity, consistency, measurability, and coverage before `/speckit-tasks` decomposes them and code is
written against them. This validates what is written, not what the code will do.
**Created**: 2026-08-19
**Feature**: [spec.md](../spec.md) | [plan.md](../plan.md) | [data-model.md](../data-model.md) |
[contracts/](../contracts/)
**Audience**: Spec author, before task generation
**Focus areas**: Model generation & judging · Reproducibility & determinism · Provenance & manifest ·
Privacy gate · Composition control · Interruption & resume · Thresholds & reporting · Cross-artifact
consistency

**Scope note**: Items reach into `plan.md`, `data-model.md`, and `contracts/` as well as the spec. A
constraint that exists only in a design document is not a requirement — an implementer working from the
spec alone would never learn it. Those items are marked `[Design-only]`.

---

## Model Generation & Judging Requirements

- [x] CHK001 FR-008 requires that the generator "MUST NOT read hidden state from the operator's environment", yet generation authenticates against a hosted model using credentials resolved from that environment. Is the credential exception stated in the requirements, or does FR-008 contradict the way the feature must actually run? [Conflict, Spec §FR-008, §Assumptions]
  - **Resolved 2026-08-19** — FR-008 amended: model credentials are the **sole** permitted environment input, declared as an access mechanism that must not influence output and must never be written to any artifact. **FR-008c** (new) closes the real hole the conflict pointed at: an environment setting that could change model selection, routing, or parameters — endpoint, profile, inference region — is genuinely the hidden state FR-008 prohibits, so it must be recorded in the manifest as a non-deterministic input, and a setting that cannot be observed causes the run to refuse rather than proceed unrecorded.
- [x] CHK002 Are requirements defined for what happens when the model **declines** a generation request on safety grounds — a case distinct from an unparseable response or a transport failure? [Gap, Spec §FR-009b, §FR-009c]
  - **Resolved 2026-08-20** — **FR-009m** (new) makes a safety decline a distinct outcome from an unparseable response and from a transport failure: the slot retries, and a slot that keeps being declined is discarded under a model-refusal reason. The requirement records why the distinction matters — conflating the two would hide a prompt domain that systematically trips a classifier behind a statistic that reads as a flaky provider.
- [x] CHK003 If a declined request is rescued by a fallback model, is it specified whether a record produced by a *different model than the one configured* is acceptable output, and how its provenance must be recorded? [Gap, Design-only, Plan §Technical Context]
  - **Resolved 2026-08-20** — **FR-009n** (new): a record produced by a model other than the one configured is acceptable output, provided FR-027a's per-record model identity is recorded and the manifest reports how many records each model served. A corpus may therefore legitimately contain records from more than one model; what matters is that no record misstates its producer and that a datasheet can report the mix.
- [x] CHK004 FR-009 requires turns to "alternate between participants" but never states **which role speaks first**. Is the opening role specified, or is it left to the implementer? [Gap, Spec §FR-009]
  - **Resolved 2026-08-20** — FR-009 now states that **the customer speaks first**: a support interaction begins with the party raising the issue. Left unstated, two conforming implementations would produce corpora differing on every single record.
- [x] CHK005 FR-009e forbids a minimum turn count "below the smallest coherent exchange". Is that floor quantified, or is it a term an implementer must invent a number for? [Ambiguity, Measurability, Spec §FR-009e]
  - **Resolved 2026-08-20** — FR-009e now quantifies the floor as **2**: one turn from each party is the smallest exchange that can be an exchange. A single turn is a statement, and no coherence rubric can meaningfully score one.
- [x] CHK006 FR-009d requires each conversation's length to be "sampled from that range as a seeded choice". Is the **distribution** across the range specified (uniform or otherwise)? Two implementations could both satisfy FR-009d and produce materially different corpora. [Gap, Spec §FR-009d]
  - **Resolved 2026-08-20** — FR-009d now names the distribution: turn counts are drawn **uniformly** from the configured range. The requirement acknowledges the simplification — uniform sampling produces more long conversations than real support traffic contains — in exchange for a range that means exactly what it says and a distribution that is testable. "Sampled from a range" alone admitted implementations that both conformed and produced materially different corpora.
- [x] CHK007 Is it specified whether the judging model may be the **same model** that generated the record, or whether self-judging is prohibited as a bias risk? [Gap, Spec §FR-009f, §FR-009j]
  - **Resolved 2026-08-20** — FR-009j now states that the judge **may** be the same model as the generator and is by default, with the self-preference risk recorded as an assumption rather than dismissed. The mitigation is empirical rather than structural: the judge scores declared rubric criteria rather than choosing between candidates, and SC-011's calibration against human judgement is what would expose a systematic bias. Pointing the two roles at different models is a configuration change, not a code change.
- [x] CHK008 FR-009h sets a threshold on a "normalized 0–1 scale". Is the score's granularity or derivation specified — continuous, or aggregated from discrete rubric criteria — such that 0.8 means the same thing across rubric revisions? [Ambiguity, Spec §FR-009h, §FR-009f]
  - **Resolved 2026-08-20** — **FR-009p** (new): the score is the **weighted mean of the judge's per-criterion scores**, using weights the rubric declares. That is what gives 0.8 a stable meaning; a holistic score would mean whatever the judging model read the prose to mean and would drift with the model. The pipeline computes the mean rather than trusting a number the model reports alongside its own sub-scores.
- [x] CHK009 FR-009l discards a record that cannot be scored "after the configured retries", but no requirement names that retry setting or relates it to FR-009c's failure tolerance. Are these one knob or two? [Clarity, Spec §FR-009l, §FR-009c]
  - **Resolved 2026-08-20** — **FR-009o** (new): one configured value governs every retryable per-record failure — declined request, unparseable or structurally invalid response, unscorable record alike — and it is explicitly distinct from transport-level retries of a single request, which FR-012d reports separately because a degraded provider and a defective generator are different facts.
- [x] CHK010 The judge is non-deterministic, so the same conversation may be admitted in one run and discarded in another. Is that variance stated as accepted behavior anywhere in the requirements, rather than only implied by the Assumptions? [Gap, Spec §Assumptions, §SC-003]
  - **Resolved 2026-08-20** — **FR-009q** (new) states the variance as accepted behaviour rather than leaving it implied by the Assumptions: because judging is a model call, the same conversation may be admitted by one run and discarded by another. The requirement ties that directly to why the rubric hash, per-record score, and judge identity are recorded — the gate is auditable though it is not deterministic — and scopes FR-010's reproducibility claim to structure and composition, deliberately not to which records survive judging.
- [x] CHK011 The spec is "English-first, not English-only", but no requirement gives the operator control over output language. Is language a configuration dimension, an emergent property of the prompt document, or undefined? [Gap, Spec §Assumptions]
  - **Resolved 2026-08-20** — **FR-009r** (new): the configuration carries the language, defaulting to English, and it is recorded in the manifest. Without an explicit setting the corpus language is whatever the prompt document happens to elicit and can drift between runs with no record of having done so. Declaring it makes a multilingual corpus a deliberate act and a monolingual one a stated fact.
- [x] CHK012 Are requirements defined for the **content** of the domain prompt document and the rubric — what each must contain to be usable — or only that they exist, are committed, and are hashed? [Gap, Spec §FR-008a, §FR-009g]
  - **Resolved 2026-08-20** — the rubric half, left open by the earlier partial resolution, is closed by **FR-009p**: the rubric must declare an identifier, a version, its criteria, and each criterion's weight, with weights summing to 1. Together with FR-008d's subdomain list for the prompt document, both committed inputs now have content requirements rather than only existence and hashing requirements.

## Reproducibility & Determinism Requirements

- [x] CHK013 FR-012b names "scenario selection" as a seeded choice, but FR-008a has the **model** elaborating scenarios from the prompt document. Are these consistent, or does one requirement seed a choice the other assigns to the model? [Conflict, Spec §FR-012b, §FR-008a]
  - **Resolved 2026-08-19** — neither requirement gave; scenario derivation is now two-level. **FR-008d** (new) requires the domain prompt document to declare an enumerable subdomain list; FR-012b's seeded choice selects the **subdomain** (so its claim is true and the corpus stratifies deterministically), and the model elaborates the specific situation within it (so FR-008a stands). FR-008b now requires both on the record: `subdomain` is reproducible from the seed, `scenario` is model text and is not. This changes the record contract — `subdomain` added to [record.schema.json](../contracts/record.schema.json) — and gives SC-013 a per-position field worth comparing.
- [x] CHK014 FR-010 and SC-003 both promise output "equivalent in structure and composition". Is "equivalent" defined operationally — which fields must match, at what granularity — so that two corpora can be objectively compared? [Measurability, Spec §FR-010, §SC-003]
  - **Resolved 2026-08-20** — **FR-010a** (new) defines it operationally: for every position present in both corpora the seeded choices match exactly — category, priority, channel, resolution status, subdomain, turn count, and ticket timestamps — and each achieved composition lies within tolerance of the other. Turn text is excluded, and so is *which* positions are present, since FR-009q makes survival through the coherence gate non-deterministic. Without the definition, SC-003 rested on whichever reading an implementer chose.
- [x] CHK015 SC-013 asserts "identical per-position seeded choices". Is the authoritative list of which choices are seeded (versus model-determined) stated in the requirements, or must it be inferred from the design? [Clarity, Spec §SC-013, §FR-012b]
  - **Resolved 2026-08-19** by the CHK013 amendment — FR-012b now names the list exhaustively ("its turn count, its composition assignment, its subdomain selection") and states explicitly that the scenario the model elaborates is model output rather than a seeded choice. **Caveat discharged 2026-08-20**: CHK016 settled the timestamps as seeded choices and FR-012b now names them, so the list is exhaustive in fact and not only in wording.
- [x] CHK016 FR-006 requires ticket creation and resolution times on every record, but no requirement says whether those timestamps are **seeded, model-chosen, or taken from the wall clock** — the distinction decides whether they are reproducible. [Gap, Spec §FR-006, §FR-012b]
  - **Resolved 2026-08-20** — **FR-006a** (new): timestamps are **seeded choices**. The configuration declares a date window, creation time is drawn from it by the same position-derived generator as every other seeded choice, and resolution time is that plus a seeded duration. Model-chosen timestamps would let narrative and metadata agree at the cost of reproducibility; wall-clock times would record when the generator ran rather than anything about the ticket. This also closes **CHK015's caveat**: FR-012b's list is now exhaustive in fact, not only in wording.
- [x] CHK017 Is it specified whether the same seed and configuration run against a **different code revision** must still be considered reproducible, or whether the code revision is part of the reproducibility contract? [Gap, Spec §FR-010, §FR-025]
  - **Resolved 2026-08-20** — **FR-010b** (new): reproducibility is claimed **within a code revision**. Two runs of the same inputs at different revisions are not required to be equivalent, and the manifest records the revision of every segment that produced a corpus (FR-015f, FR-025a), so a difference is explained rather than denied. Claiming reproducibility across arbitrary revisions would be a promise no change to the generator could keep.
- [x] CHK018 SC-001 requires the run to complete "within an operator-acceptable window recorded in the configuration", but no functional requirement establishes such a field, and the configuration described in [data-model.md](../data-model.md) has none. Is this criterion satisfiable as written? [Conflict, Spec §SC-001, Design-only]
  - **Resolved 2026-08-19**, together with CHK059 — **FR-012f** (new) lets the configuration declare a run budget: a maximum wall-clock duration, a maximum number of model calls, or both. Exhausting one **stops and checkpoints** rather than failing, so no completed work is lost and resuming stays the operator's decision; the manifest records the declared budget and the actual spend. SC-001 now refers to that budget, making the criterion literally satisfiable, and an unattended release-scale run finally has a ceiling on time and cost.
- [x] CHK019 Are requirements defined for whether a corpus generated at one concurrency level and one generated at another must be **byte-comparable in record order**, or only comparable position by position? [Clarity, Spec §FR-012c, §SC-013]
  - **Resolved 2026-08-20** — FR-012c now specifies both halves: records are written in **ascending record position**, and serialization is deterministic so identical content yields identical bytes. It also states what is *not* claimed — two corpora are never byte-comparable as files, because turn text differs by FR-010; the comparison that matters is per position, as FR-010a defines it.

## Provenance, Manifest & Traceability Requirements

- [x] CHK020 FR-029 requires a record to be traceable to its manifest "using only the record's own fields", but no requirement establishes how the manifest is **located** — its path, filename convention, or co-location with the artifact. Is traceability achievable as specified? [Gap, Spec §FR-029, §FR-025]
  - **Resolved 2026-08-19** — **FR-029a** (new) requires the manifest to be written beside its artifact and **named by the run identifier**, so a record's own `run_id` locates the file directly. The requirement records why: without a naming rule FR-029's claim is unachievable, because a record carries an identifier rather than a path, and someone holding a single record would have no way to reach the run that produced it — precisely the investigation FR-029 exists to support. The manifest also names the artifact it describes (FR-025b), so the mapping is navigable in both directions.
- [x] CHK021 FR-025 enumerates what the manifest records but omits **run start and end times**, which Principle II names explicitly as non-deterministic inputs to capture. Is the omission deliberate? [Gap, Spec §FR-025, Constitution §II]
  - **Resolved 2026-08-19** — FR-025 now requires the times the run started and completed, closing the omission against Principle II, which names wall-clock time as a non-deterministic input to capture. The requirement also states the second reason: elapsed time is part of what a later reader needs in order to judge whether a run can be repeated.
- [x] CHK022 Are requirements defined for what the manifest records when the **code revision cannot be determined** or the working tree is dirty, or is that decision left entirely to the design? [Gap, Design-only, Spec §FR-025]
  - **Resolved 2026-08-19** — **FR-025a** (new) promotes research R10 from a design choice to a requirement: the code revision is recorded with an explicit modified-tree indicator, and an unavailable revision records why. Neither fails a run — a revision recorded from a modified tree silently misrepresents what produced the artifact, so the indicator makes the condition reviewable, while refusing outright would block ordinary development. Weighing the caveat belongs to the separate act of deciding to release.
- [x] CHK023 FR-003 requires a stable record identifier but says nothing about its **derivation or uniqueness scope** — unique within an artifact, within a run, or globally across corpora? [Clarity, Spec §FR-003, §FR-015b]
  - **Resolved 2026-08-19** — **FR-003a** and **FR-003b** (new). The run identifier is generated fresh per run instance and carried in the checkpoint, so a resume continues under the identifier it started with while a rerun — even with identical seed and configuration — gets a new one; deriving it from the inputs would make two legitimate reruns indistinguishable and give two corpora the same record identifiers. The record identifier is derived deterministically from the run identifier and the record's position, making uniqueness within a run structural, uniqueness across runs a consequence, and FR-015b true by construction rather than by checking.
- [x] CHK024 FR-027 requires recording "any generation model identity". When a fallback causes different records in one corpus to come from different models, is a single run-level identity sufficient, or is per-record model identity required? [Gap, Spec §FR-027]
  - **Resolved 2026-08-19** — **FR-027a** (new) requires each record to carry the identity of the model that **actually** produced it whenever the serving model may differ from the configured one, with the manifest additionally reporting how many records each model served. The requirement states the reasoning: a single run-level identity would be false for part of the corpus, and provenance that is false for an unidentifiable subset is worse than provenance that is absent.
- [x] CHK025 FR-026 requires that input count minus discards equals output count, but the term "input count" is never defined for a generator that creates its own inputs. Is it the requested corpus size, the number of model responses received, or the number of slots attempted? [Ambiguity, Measurability, Spec §FR-026, §SC-005]
  - **Resolved 2026-08-19** by the CHK053 amendment — FR-026a defines the term the reconciliation depends on: records generated is every response received from the generating model, counted once per attempt, and each is either written or discarded under exactly one reason. "Input count" for a generator that creates its own inputs now has one meaning.
- [x] CHK026 Is the set of permitted **discard reasons** established by the requirements, or does the spec leave reasons open-ended while FR-026's reconciliation depends on them being enumerable? [Gap, Design-only, Spec §FR-026]
  - **Resolved 2026-08-19** — **FR-026b** (new) enumerates the closed set of discard reasons normatively in the spec, upgrading the earlier partial resolution. FR-026's reconciliation is now a sum over a fixed vocabulary rather than over free text, and using a reason outside the set is invalid.
- [x] CHK027 FR-028 requires the manifest to be "validatable" and to name missing elements. Is it specified whether validation also enforces the FR-026 reconciliation arithmetic, or only field presence? [Clarity, Spec §FR-028, §FR-026]
  - **Resolved 2026-08-19** — FR-028 now requires validation to check the FR-026 reconciliation arithmetic and name the discrepancy, not merely to confirm that fields are present. The requirement says why: a manifest whose fields are all present but whose accounting does not balance is not valid, and presence checking alone would pass exactly the manifests worth catching.
- [x] CHK028 Is a requirement stated that the manifest bind to a **specific output file** (for example by checksum), so that a manifest cannot be read beside an artifact it does not describe? [Gap, Design-only, Spec §FR-025]
  - **Resolved 2026-08-19** — **FR-025b** (new) requires the manifest to identify its artifact by filename and content checksum, so a manifest cannot be read beside a file it does not describe and a corpus altered after the fact is detectable.

## Privacy Gate Requirements

- [x] CHK029 FR-021 discards flagged records, while FR-022 lets a reviewer approve a finding as a legitimate synthetic value. If the flagged record was discarded and never written, and FR-020 forbids reporting the matched value, **what does the reviewer inspect in order to approve it**? Are the two requirements jointly satisfiable? [Conflict, Spec §FR-021, §FR-022, §FR-020]
  - **Resolved 2026-08-19** — they were not jointly satisfiable as written. Spec amended: **FR-020a** adds a deterministic, irreversible **masked rendering** to every finding (domain for an email, issuer range for a card, shape and length otherwise), which settles the common synthetic cases without reproducing the value; **FR-021b** retains privacy-discarded records in a **quarantine artifact** under `data/interim/`, never committed and never dataset output, for findings a mask cannot settle. FR-022 now states that a reviewer must be able to decide from one or the other and need not have observed the original run. Assumptions record why quarantine is compatible with Principle IV: the content is fabricated and merely identifier-shaped, and the requirement is about the provenance of content, not its shape.
- [x] CHK030 Is the authoritative list of **scanned fields** stated in the requirements? The record carries model-generated free text in `scenario` as well as in turn content; the Edge Cases say "every field carrying free text" without naming them. [Gap, Spec §FR-023, §Edge Cases]
  - **Resolved 2026-08-19** — **FR-023a** (new) names the scanned fields: every conversation turn's content and the elaborated scenario, the record's model-derived text. Pipeline-assigned fields and the **subdomain** are excluded by requirement rather than by omission, because an identifier can only enter through model output and scanning UUIDs and hashes would generate findings every run would then have to except. The residual risk is stated in the requirement itself: a prompt document containing a real identifier is not caught by this gate, only by review of a committed file.
- [x] CHK031 Does FR-018's term "government identifiers" match what an offline detector can actually cover (in practice, US SSN), or does it promise coverage of non-US identifiers that nothing detects — the exact overclaim FR-019 exists to prevent? [Conflict, Spec §FR-018, §FR-019]
  - **Resolved 2026-08-19** — FR-018 now names **US Social Security numbers** rather than "government identifiers", stating the floor at the level of the identifier type actually detected; naming the broad category promised coverage of non-US identifiers that no offline detector delivers, which is the same overclaim FR-019 exists to prevent, merely relocated into the requirement. FR-019 is strengthened in turn: every report enumerates the types the scan **covers** as well as those it does not, at that same specificity, so widening coverage later is visible as a change in what the report claims rather than a silent improvement.
- [x] CHK032 The design places the privacy scan **last**, after coherence judging, so a record discarded for incoherence is never scanned. Is it specified whether privacy scanning must cover records that failed an earlier gate, and does the FR-021a discard rate become unrepresentative if it does not? [Consistency, Coverage, Spec §FR-016, §FR-021a, Design-only]
  - **Resolved 2026-08-19** — **FR-016a** (new) moves the scan ahead of the judge: it runs on every structurally valid response. The under-measurement this item identified is real and is now closed — PII emission is measured across all usable output rather than only the part that survived the coherence gate, which matters most exactly when the generator is worst. Scanning early also stops the pipeline paying for a judge call on a record about to be discarded for privacy. FR-016 still holds: nothing unscanned reaches the release path.
- [x] CHK033 Are requirements defined for **who may approve** a privacy exception, and whether the person who ran the generator may approve findings on their own output? [Gap, Spec §FR-022]
  - **Resolved 2026-08-19** — **FR-022a** (new): every exception records who approved it and when. Self-approval is permitted deliberately — requiring a second person would make the mechanism unusable on a single-maintainer project, and an unusable control is not a control — with the check relocated rather than removed: every active exception must appear in the datasheet of any release relying on it.
- [x] CHK034 Is there a requirement that an approved exception be re-reviewed or expire, or do approvals persist indefinitely once recorded? [Gap, Spec §FR-022]
  - **Resolved 2026-08-19** — **FR-022a**: approvals do not expire. Review is tied to the release act, where the datasheet must list every active exception, rather than to a timer that would expire approvals between two otherwise identical runs.
- [x] CHK035 Is it stated that the exception's free-text reason must not itself contain the matched value, given that keeping values out of the repository is the point? [Gap, Spec §FR-022, §FR-020]
  - **Resolved 2026-08-19** — **FR-022b** (new) requires the stated reason to be free of the matched value and any other identifier, **and** requires the detectors to be run over the reason text, refusing one that trips them. A free-text field that may hold a value would otherwise defeat the fingerprinting it sits beside.
- [x] CHK036 The spec's privacy categories are all blocking; the design introduces **advisory, non-blocking** categories. Is the advisory tier a requirement, and is it unambiguous which categories block? [Gap, Design-only, Spec §FR-018]
  - **Resolved 2026-08-19** — **FR-018b** (new) makes the advisory tier a requirement: a category may be registered as reported-but-never-blocking, and every report must state each category's blocking status. Neither direction is left to inference — an advisory finding cannot be read as a gate that passed, and a blocking category cannot be assumed advisory.
- [x] CHK037 Are requirements defined for what happens when a detector **raises an error** mid-scan — fail closed, skip the record, or abort the run? [Gap, Exception Flow, Spec §FR-017]
  - **Resolved 2026-08-19** — **FR-017a** (new): a detector that raises causes the record to be treated as flagged and discarded, failing closed under **`detector_error`**, a reason deliberately distinct from `privacy_finding` so a malfunction is never recorded as either a clean result or a real identifier. Repeated failures stop the run. The reason was added to FR-026b's closed set.
- [x] CHK038 Can "a detector set that cannot cover this floor" be objectively evaluated — is the comparison against *declared* categories or against *demonstrated* detection capability? [Measurability, Spec §FR-018]
  - **Resolved 2026-08-19** — **FR-018a** (new) settles it as **demonstrated**: each floor type is probed at run start with committed synthetic canaries, and the run proceeds only if every type is actually detected. This closes the exact failure a declaration check cannot see — a detector whose pattern silently stopped matching still declares its category and would otherwise pass the gate and report clean.
- [x] CHK039 Does FR-024's "without contacting a network service" also cover **telemetry and usage reporting** by a detection library, or only detection lookups? [Clarity, Spec §FR-024]
  - **Resolved 2026-08-19** — FR-024 now covers telemetry, analytics, and usage reporting as well as detection lookups, and requires the pipeline to disable such reporting explicitly rather than relying on a dependency's default staying unchanged. The point is not only offline operation but that the gate cannot transmit the content it examines.

## Composition Control Requirements

- [x] CHK040 FR-031's ±2 percentage point tolerance is stated "per controlled dimension". Is it per **member** of a dimension or an aggregate over the dimension? The two readings can pass and fail the same corpus. [Ambiguity, Measurability, Spec §FR-031]
  - **Resolved 2026-08-19** — FR-031 now states the tolerance is evaluated **per member of each dimension**: every member's achieved proportion must sit within the tolerance of its request, a dimension passes only when its worst member passes, and a failure names the offending member and its drift rather than the dimension. Aggregate measures were rejected in the requirement itself, with the reason recorded: someone slicing the corpus by one category cares about that category, and an average lets a badly-served member hide behind well-served ones. SC-008 was reworded to match.
- [x] CHK041 FR-030 treats the four dimensions independently. Are **cross-dimension** constraints required — combinations that should not co-occur, or joint distributions the operator can express — or is independence an accepted simplification? [Gap, Spec §FR-030]
  - **Resolved 2026-08-19** — independence is now stated rather than implied. A new assumption records that the four dimensions are apportioned separately, that any combination may therefore occur, and that an implausible pairing is the generating model's problem to render coherently and the coherence judge's to catch — not a composition concern. The tradeoff is explicit: four simple distributions instead of a 384-cell cross-product, at the cost of not being able to forbid a specific pairing.
- [x] CHK042 FR-033 requires "a documented default distribution", but the spec never states its values; they exist only in [data-model.md](../data-model.md). Is the default itself a requirement, or an implementation choice that may change without a spec change? [Traceability, Design-only, Spec §FR-033]
  - **Resolved 2026-08-19** — the default distribution and the 4–12 turn range are now stated **in FR-033 itself**, as a table, with the requirement noting they are requirements rather than implementation choices: changing them changes the corpus every unconfigured run produces. `data-model.md` now restates them and says the spec is normative.
- [x] CHK043 The Assumptions concede that ±2pp "may be unreachable on very small corpora". Is a requirement defined for that case — refuse, warn, or silently exceed the tolerance and fail the run? [Gap, Spec §FR-031, §Assumptions]
  - **Resolved 2026-08-19** — **FR-031b** (new) makes achievability a precondition. Assigning whole records bounds per-member error at `1 / record_count` before any discard, so a tolerance below that is unsatisfiable by arithmetic; the run refuses before generating and states both the minimum corpus size and the minimum tolerance that would work. The requirement is explicit that this is a necessary condition, not a sufficient one — meeting it does not guarantee the tolerance survives discards. The Assumptions were rewritten accordingly, and `configs/smoke.toml` now declares a 10pp tolerance because 20 records cannot achieve 2pp.
- [x] CHK044 FR-032 requires refusing an unsatisfiable request. Are the conditions that make a request unsatisfiable **enumerated** (proportions that do not sum, unknown members, a corpus too small to round), or left to the implementer to discover? [Clarity, Spec §FR-032]
  - **Resolved 2026-08-19** — FR-032 now enumerates the four conditions that make a request unsatisfiable: proportions that do not sum to 1 within a stated epsilon, a value that is not a member of the dimension's enumeration, a proportion too small to round to a whole record at the requested corpus size, and a tolerance unachievable at that size (FR-031b).
- [x] CHK045 When discards perturb the achieved composition past tolerance, is it specified whether the run fails for *composition* or for the underlying discard cause — and can an operator tell the two apart from the report? [Consistency, Spec §FR-031, §FR-009k, §FR-021a]
  - **Resolved 2026-08-19** — **FR-031a** (new) requires **three** distributions per dimension rather than two: requested, assigned, and achieved. Requested → assigned exposes apportionment error; assigned → achieved exposes drift caused by discards. Without the middle term a tolerance failure has no attributable cause, and the two causes call for entirely different responses. The manifest and report carry all three, plus a per-member breach list.

## Interruption, Resume & Output Path Requirements

- [x] CHK046 FR-015e refuses a resume when the configuration, seed, prompt document, or rubric differ — but not when the **code revision** differs. Is resuming under changed code acceptable, given it produces exactly the mixed provenance FR-015e exists to prevent? [Gap, Spec §FR-015e]
  - **Resolved 2026-08-20** — **FR-015f** (new): a changed code revision does **not** refuse a resume. The manifest instead records one segment per run or resume, each with its own code revision and record range, so a corpus produced across more than one revision is described honestly rather than prevented. The requirement states the trade: refusing would discard the completed part of a long run over an edit made while it was interrupted, and this is the same trade FR-025a already makes for a modified working tree. The code revision is deliberately excluded from the checkpoint's input fingerprint set for the same reason.
- [x] CHK047 FR-015b forbids reusing "a record identifier already issued", while a retried slot legitimately regenerates the same position. Is it unambiguous whether re-issuing an identifier for a **discarded** record counts as reuse? [Ambiguity, Spec §FR-015b, §FR-009c]
  - **Resolved 2026-08-20** — FR-015b now defines the term: an identifier counts as **issued when a record carrying it has been written**. Re-deriving the identifier of a position whose earlier attempts were all discarded is therefore not reuse, since no record ever bore it. That definition is what lets FR-009c's retries and FR-003b's deterministic derivation coexist with the no-reuse rule instead of contradicting it.
- [x] CHK048 The Edge Cases require an unusable checkpoint to be "reported as such". Are requirements defined for what the operator may then do — what happens to partial output, and whether restarting must first clear it? [Gap, Recovery Flow, Spec §Edge Cases]
  - **Resolved 2026-08-20** — **FR-015g** (new): an unreadable checkpoint is reported as unusable and refuses the resume, and the partial output is **preserved** rather than discarded. Restarting from scratch is an explicit operator action, because the alternative is a tool that silently decides to throw away completed work at the precise moment its own state became untrustworthy.
- [x] CHK049 Are requirements defined for **two runs sharing a checkpoint** — the same configuration and seed started twice concurrently — or is that failure mode unaddressed? [Gap, Coverage]
  - **Resolved 2026-08-20** — two requirements, because CHK023's fresh-run-identifier decision had split this into two problems. **FR-014a** (new) claims the destination at run start and re-verifies immediately before the artifact is placed there: checking only at the start lets two concurrent runs both pass and the second silently replace the first's output at the end. **FR-015h** (new) makes resume identify its run — candidates are found by matching input fingerprints, one match resumes, several refuse and are named, none refuses rather than starting a new run under the guise of resuming.
- [x] CHK050 FR-015 requires interrupted output to be distinguishable from finished output. Is the *mechanism* constrained by requirements (for instance, that incomplete output must not occupy the release path at all), or is the design free to satisfy it with a naming convention? [Clarity, Design-only, Spec §FR-015, §FR-013]
  - **Resolved 2026-08-20** — FR-015 now constrains the mechanism, not just the outcome: incomplete output must not occupy the release path **at all**, and the artifact appears there by a single indivisible operation. The requirement records why location beats convention — distinguishing complete from incomplete by directory cannot be defeated by a partial write, a crash, or a naming mistake, as a suffix convention can. The constitution's separation of directories was already doing this work; it is now required rather than merely relied upon.
- [x] CHK051 FR-014 forbids silently overwriting or appending to an existing artifact. Is a deliberate, explicit overwrite path required, or is overwriting prohibited outright? [Gap, Spec §FR-014]
  - **Resolved 2026-08-20** — FR-014 answers it outright: **no overwrite option exists**. The run refuses and names the conflicting path. The reasoning is recorded in the requirement: the data directory is outside version control, so an overwritten corpus and its manifest are unrecoverable, and removing a release artifact should stay a deliberate manual act rather than a flag that can find its way into a script that runs unattended.
- [x] CHK052 Are requirements defined for the **retention or cleanup** of checkpoints and partial output after a successful or abandoned run, or do they accumulate indefinitely under `data/`? [Gap, Spec §FR-015a, Constitution §Technology & Data Constraints]
  - **Resolved 2026-08-20** — **FR-015i** (new): on success the staging copy and checkpoint are removed, since the artifact in the release path supersedes them, while the run report and any quarantine artifact are retained — quarantine is the input to FR-022's approval and cannot be reconstructed without regenerating the corpus. A failed or interrupted run retains everything, which is exactly when it is needed.

## Thresholds, Reporting & Run Outcome Requirements

- [x] CHK053 FR-021a and FR-009k both express a threshold as a percentage "of records generated". Is the **denominator** defined — slots attempted, model responses received including retries, or records written? Each yields a different rate from the same run. [Ambiguity, Measurability, Spec §FR-021a, §FR-009k]
  - **Resolved 2026-08-19** — spec amended with **FR-026a**, one definition governing every rate expressed as a proportion of records generated: *every response received from the generating model, counted once per attempt*. A slot retried three times contributes three. This is the only reading under which FR-026's reconciliation closes, since every counted response either becomes a written record or is discarded under exactly one reason. The consequence is recorded in the requirement rather than left to be discovered: heavy retries enlarge the denominator and therefore **dilute** both discard rates.
- [x] CHK054 Is it specified **when** run-level thresholds are evaluated — continuously so a doomed run stops early, or only at completion? At release scale the difference is most of the cost of the run. [Gap, Spec §FR-009k, §FR-021a, §SC-001]
  - **Resolved 2026-08-20** — **FR-037** (new): the privacy and coherence discard rates are evaluated **during** the run, once at least 1,000 records or 5% of the requested corpus exist, whichever is larger; a breach stops the run, checkpoints, and reports failure. Evaluating only at completion meant a generator emitting identifiers on every record still cost a full release-scale run before anyone was told, and the minimum sample is what stops an early cluster of discards failing a run that would have been fine. **FR-037a** keeps the composition tolerance end-only: a partial corpus has no achieved composition, so an early check would measure incompleteness rather than drift.
- [x] CHK055 FR-036 requires the run to "signal success or failure unambiguously". Is the outcome taxonomy specified — refused before generating, failed a threshold, interrupted — or only a binary? These call for different operator responses. [Clarity, Spec §FR-036]
  - **Resolved 2026-08-20** — **FR-036b** (new) defines four outcomes rather than a binary: **completed**, **refused** (nothing generated, nothing spent), **failed** (output exists but did not qualify, with accounting explaining why), and **stopped** (interrupted, budget exhausted, or halted by a threshold — resumable, work preserved). They call for different responses, which is precisely what a success/failure bit cannot carry. The CLI's four exit statuses map onto them, and the report carries the same value for automation that should not be reading exit codes.
- [x] CHK056 Is the report's **format and location** established by the requirements, or only that it be machine-readable? [Gap, Spec §FR-035, §FR-036]
  - **Resolved 2026-08-20** — **FR-036a** (new): the report is **JSON**, named by run identifier, written beside the artifact on success and into the run's intermediate directory otherwise. Both halves matter — a format nobody agreed on cannot be consumed by automation, and a report whose location depends on how the run ended would be hardest to find exactly when it is most needed.
- [x] CHK057 SC-009 requires reporting the score distribution across the corpus. Is the distribution's form specified — bucketing, summary statistics — well enough to be objectively produced and compared? [Measurability, Spec §SC-009]
  - **Resolved 2026-08-20** — **FR-038** (new): counts in fixed 0.05 buckets across the 0–1 range, plus count, minimum, maximum, mean, and median. Fixed buckets are the point: a free choice of bucketing would make every run's distribution incomparable with every other, which defeats what SC-009 is for.
- [x] CHK058 FR-034 requires reporting duplicate conversations. Is the **scope** of duplicate detection specified — within a single run only, or across previously generated corpora — and is the comparison basis (turn content, whole record) defined? [Clarity, Spec §FR-034]
  - **Resolved 2026-08-20** — **FR-039** (new) settles both halves. Scope is **within a single run**; comparison against earlier corpora would need a persistent cross-run registry the feature does not otherwise require, and FR-034's purpose is a diversity signal for the run in hand. The comparison basis is the Unicode-normalized turn sequence — role and content in order — with metadata and identifiers excluded, since assigned metadata varies by construction and would mask the repetition the count exists to surface.
- [x] CHK059 Are requirements defined bounding a run's **cost or call volume** — a ceiling, an estimate before starting, or an abort condition? SC-001 asserts a 100,000-record run is achievable; nothing prevents an unattended run from spending far more than intended. [Gap, Spec §SC-001, §Assumptions]
  - **Resolved 2026-08-19** — closed by FR-012f alongside CHK018: a declared budget of wall-clock time, model calls, or both, with exhaustion stopping and checkpointing the run.

## Cross-Artifact Consistency & Governance

- [x] CHK060 [contracts/record.schema.json](../contracts/record.schema.json) requires `resolved_at` to be **absent unless** the resolution status is `resolved`. FR-006 requires only that the record carry creation and resolution times. Is the conditional a requirement, or a design decision that narrows the contract? [Consistency, Design-only, Spec §FR-006]
  - **Resolved 2026-08-20** — **FR-006b** (new) promotes the contract's conditional to a requirement: a resolution time is present when and only when the status is *resolved*. A ticket that was escalated, abandoned, or left unresolved has no resolution to time, and recording one anyway would make the field mean different things in different records.
- [x] CHK061 The record contract admits a minimum of two turns while the configured default range starts at four. Is the contract-level minimum a requirement in its own right, and is it consistent with FR-009e's "smallest coherent exchange"? [Consistency, Spec §FR-009e, Design-only]
  - **Resolved 2026-08-20** — closed by the CHK005 amendment rather than by a new one. FR-009e now sets the floor at **2**, which is exactly the record contract's `minItems`, so requirement and contract agree. The default range starting at 4 is a configuration default (FR-033), not a second minimum — the inconsistency this item named no longer exists.
- [x] CHK062 The design records a coherence score and rubric identifier on every record. FR-009i requires the score; is the **rubric identifier on the record** a requirement, given a score is uninterpretable without it? [Gap, Spec §FR-009i, §FR-009g]
  - **Resolved 2026-08-20** — FR-009i now requires the **rubric identifier** on the record alongside the score. It is not optional detail: the criteria and weights behind a score live in the rubric (FR-009p), so a bare number cannot be compared across rubric versions or interpreted without one.
- [x] CHK063 SC-011 requires the judge to be calibrated against human judgment "before a corpus is released". Is calibration a **gate** with an owner and a recorded artifact, or an advisory expectation no requirement enforces? [Ambiguity, Spec §SC-011, Constitution §V]
  - **Closed 2026-08-20 as an accepted deferral** — no amendment. SC-011 stays an expectation: the `sample-for-review` command exists, calibration remains a documented practice, and no requirement obliges a calibration record to exist or a release to cite one. **The accepted consequence, stated plainly**: whether the judge was ever calibrated, and against which rubric version, leaves no trace anyone can check from the repository, so "is the 0.8 threshold validated?" is unanswerable from the artifacts alone. The remedy if that becomes a problem is a committed calibration record that a datasheet must reference; noted in `quickstart.md` at the calibration scenario.
  - **Partially reversed 2026-08-20.** The remedy above is now built: `ticket-dataset calibrate` compares a hand-scored sample against the judge and writes a committed record under `calibration/`, named by `rubric_id` so a release can find the one matching its corpus. **The deferral itself still stands** — nothing obliges the record to exist or a release to cite it, so the accepted consequence is unchanged. What changed is that the check is now a lookup rather than a project to start, should anyone decide to require it.
  - **Why it came due**: two live runs against a self-hosted model produced **three distinct coherence scores across twenty records**, 70% of them a perfect 1.0. The cause was the rubric, not the model — v1 offered exactly three anchors per criterion, so a judge asked to pick an anchor picked the top one, and the 0.8 threshold could not separate anything between 0.85 and 1.0. Rubric v2 replaces the anchors with a continuous scale. That is precisely the finding calibration exists to surface, and it surfaced without a human ever scoring a record.
- [x] CHK064 SC-010 claims a new contributor can generate their first corpus "using the project's documentation alone". Is the documentation in scope for that claim identified, and is any requirement responsible for producing it? [Measurability, Spec §SC-010]
  - **Resolved 2026-08-20** — SC-010 now names its scope: `README.md` and the feature quickstart, which between them must cover installation, how credentials are supplied, how a configuration is written, and one worked run producing a corpus. An unbounded claim about "the project's documentation" could neither be satisfied nor falsified.
- [x] CHK065 Is the assumption "conversations are fabricated, not derived from real support transcripts" enforced by any requirement, given it is the stated basis for accepting the residual privacy risk that FR-019 declares? [Assumption, Spec §Assumptions, §FR-019]
  - **Closed 2026-08-20 as an accepted deferral** — no amendment. Synthetic-by-construction stays an assumption. The reasoning is that the pipeline has no input that could carry a real transcript: its inputs are a configuration, a seed, a committed prompt document, and a committed rubric, and no code path ingests transcript data. **The residual exposure, stated honestly**: nothing prevents a person from pasting real content into a committed prompt file, and FR-023a already excludes those files from the scan. The control is review of a small committed file, not a mechanism.
- [x] CHK066 Are the requirements internally consistent about scope — does anything still imply that this feature validates externally postprocessed datasets, which was explicitly moved to feature 002? [Consistency, Spec §Assumptions]
  - **Closed 2026-08-20 by decision** — the overlap is deliberate and now recorded in the spec's Assumptions and in `cli.md`. The standalone scan accepts any JSONL path because the approval loop needs a cheap re-check after the exception file changes, and re-running generation to verify one approval would cost two model calls per record. The overlap is accepted **on the expectation that feature 002 reuses this implementation rather than growing a second one** — worth carrying into 002's re-specification. The boundary that still holds is ownership: this feature owns the blocking gate over its own output; 002 owns judging a dataset of uncertain provenance as a whole.
  - **Swept 2026-08-19, still open** — the spec itself is clean: the only mentions of externally postprocessed datasets are scope *exclusions* (the Input description and the Assumptions), and FR-016 scopes the scan to "its own generated output". The leak is in the design: [cli.md](../contracts/cli.md) defines `ticket-dataset privacy scan PATH` over an arbitrary existing artifact, which is a scan of a file this feature did not necessarily produce. It is needed by the approval loop (re-checking after the exception file changes), so the question is whether to scope it to this project's own artifacts or accept it as a deliberate overlap with feature 002. [Design-only]

## Notes

- Check items off as resolved: `[x]`. An item may be resolved by **amending the spec** or by recording a
  deliberate decision that the gap is acceptable — but not by leaving it unexamined.
- **All five `[Conflict]` items are resolved** — CHK001, CHK013, CHK018, CHK029, CHK031 — each by amending
  the requirements rather than by deciding how to implement around them. Each was two requirements
  disagreeing, so no amount of careful implementation would have resolved any of them; an implementer would
  simply have picked a side without noticing there was one.
- **Resolved on 2026-08-19**: CHK001, CHK013, CHK018, CHK029, CHK031, CHK053, CHK059 (as part of CHK018),
  and the whole composition cluster — CHK040 through CHK045. A sweep of the remaining items then closed
  **CHK015** and **CHK025**, which the CHK013 and CHK053 amendments had already resolved without anyone
  noticing, and annotated five more as partially resolved (CHK012, CHK026, CHK038, CHK061, CHK065) — a
  partially resolved item is not resolved, and stays open. The sweep also found one thing the checklist had
  not: `privacy scan PATH` in the CLI contract scans an arbitrary artifact, which is the only place this
  feature's scope leaks toward feature 002 (see CHK066).
- **Provenance cluster resolved 2026-08-19** — CHK020 through CHK028. Seven of the nine were constraints
  living only in `plan.md`, `data-model.md`, or `contracts/`; they are now requirements (FR-003a, FR-003b,
  FR-025 amended, FR-025a, FR-025b, FR-026b, FR-027a, FR-028 amended, FR-029a), so a future revision cannot
  drop them without a requirement noticing. The one that was not merely a promotion is CHK020: FR-029's
  traceability claim was unachievable as written, because a record carries an identifier and not a path.
  Naming manifests by run identifier makes the claim literally true.
- **Privacy governance cluster resolved 2026-08-19** — CHK030, CHK032 through CHK039. Two changed behaviour
  rather than wording: the scan moved ahead of the coherence judge (FR-016a), because a privacy rate computed
  only over records that survived judging under-reports exactly when the generator is worst; and floor
  coverage became a demonstration against committed canaries rather than a reading of a detector's declared
  categories (FR-018a), which is the only way to catch a detector that still declares a type it no longer
  detects. The approval regime (FR-022a) deliberately permits self-approval and relocates the second pair of
  eyes to the release datasheet, on the reasoning that an unusable control is not a control.
- **Interruption and resume cluster resolved 2026-08-20** — CHK046 through CHK052. Two were consequences of
  earlier decisions rather than original gaps: CHK047 needed "issued" defined before FR-003b's deterministic
  identifiers could coexist with FR-015b, and CHK049 became two problems once CHK023 made run identifiers
  fresh per run — nothing contends for a checkpoint any more, but two runs still contend for an output path,
  and resume lost its unambiguous target. FR-015f is the notable judgement: a changed code revision is
  recorded as a manifest segment rather than refusing the resume, on the same reasoning FR-025a uses for a
  modified working tree.
- **Generation and judging cluster resolved 2026-08-20** — CHK002 through CHK012. Most were requirements
  that stopped one step short of being testable: "alternate between participants" without naming who opens,
  "sampled from that range" without naming the distribution, "smallest coherent exchange" without a number,
  "normalized 0–1" without a derivation. Each is now specific enough that a test can fail. Two carry accepted
  risks stated rather than engineered away: the judge shares the generator's model by default (FR-009j, with
  SC-011 calibration as the check on self-preference), and uniform turn-count sampling produces longer
  conversations than real support traffic (FR-009d).
- **Loose ends swept 2026-08-20** — CHK014, CHK016, CHK017, CHK019, CHK060, CHK061, CHK062 resolved by
  amendment; CHK061 needed none, having been closed by CHK005's floor of 2. CHK016 also discharged CHK015's
  caveat: with timestamps seeded, FR-012b's list of seeded choices is exhaustive in fact. **Three were closed
  as accepted deferrals rather than fixed** — CHK063 (calibration leaves no checkable trace), CHK065
  (synthetic-by-construction stays an assumption; a person can still paste real content into a committed
  prompt file), and CHK066 (privacy scanning of arbitrary paths is deliberate shared surface with feature
  002, on the expectation that 002 reuses rather than reimplements). Each carries its accepted consequence
  in writing, which is the point of closing an item this way rather than leaving it unexamined.
- **Reporting cluster resolved 2026-08-20 — checklist complete.** CHK054 through CHK058 and CHK064. FR-037
  is the one with money attached: discard-rate thresholds are now evaluated during the run after a minimum
  sample, so a defective generator costs roughly 5% of a release-scale run instead of all of it, while the
  composition tolerance stays end-only because a partial corpus has no achieved composition to measure.
  FR-036b replaces a success/failure bit with four outcomes, since "nothing was spent", "output exists but
  did not qualify", and "work is preserved and resumable" are different facts an operator acts on
  differently.

## Status

**All 66 items closed as of 2026-08-20.** Sixty-three were resolved by amending the requirements; three —
CHK063, CHK065, CHK066 — were closed as accepted deferrals with their consequences recorded above. Five
began as `[Conflict]`: requirements that contradicted each other and that no amount of careful
implementation would have reconciled. The spec grew roughly forty new or amended requirements in the
process, and the record contract gained two fields (`subdomain`, and the rubric identifier beside the
coherence score).

Nothing here blocks task generation. An item closed as a deferral remains a decision that can be revisited;
what it must not become again is an unexamined gap. Spec amendments: FR-008 (credential carve-out), FR-008c, FR-008d, FR-012f, FR-018, FR-019,
  FR-020a, FR-021b, FR-022, FR-026a, SC-001, plus Assumptions and Edge Cases. `subdomain` was added to the
  record contract, and FR-031, FR-031a, FR-031b, FR-032, FR-033, and SC-008 were rewritten for the
  composition cluster. Every change is propagated through `research.md`, `plan.md`, `data-model.md`,
  `contracts/`, and `quickstart.md`.
- `[Design-only]` items mark constraints that exist in `plan.md`, `data-model.md`, or `contracts/` but not
  in the spec. A design document is not a requirement: an implementer following the spec alone would never
  learn them, and a future revision could drop them without any requirement noticing.
- `[Ambiguity]` items concentrated on denominators and tolerances (CHK040, CHK053) are worth resolving
  before tasks are written — a threshold whose denominator is undefined cannot be tested, and the tests
  will encode whichever reading the implementer happened to pick.
- `[Gap]` items may be legitimate deferrals. The point is that the deferral becomes explicit and dated
  rather than discovered mid-implementation.
- This checklist tests the requirements, not the implementation. Nothing here should be resolved by
  writing code.
